[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "fdtd-acoustic-3d"
version = "0.0.1"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["fdtd*", "visualisation*", "voxelization*"]